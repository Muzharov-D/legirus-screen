// DEPRECATED — компонент удалён в Sprint 2 cleanup (не импортируется никем).
// Заменён в ClubOverview.jsx собственным «halftime-team» блоком.
// Файл оставлен пустым stub-ом — физическое удаление через `del` / `git rm`.
// См. HANDOFF.md → "Файлы на физическое удаление".
export default function HalfTimeBars() { return null; }
