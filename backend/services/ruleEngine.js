// DEPRECATED — rule-based ИИ-агент удалён в Sprint 2 cleanup.
// Будет заменён реальным LLM (Claude/GPT) в отдельной итерации.
// Файл-stub чтобы не ломать возможные исторические импорты — экспортирует
// no-op, всегда возвращающий null. Физическое удаление через `del`.
export function generateInsight() {
  return null;
}
